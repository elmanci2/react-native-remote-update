<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.remoteupdate">

    <application
        android:name=".YourApplicationName"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher">

        <receiver android:name=".BundleReceiver">
            <intent-filter>
                <action android:name="com.remoteupdate.ACTION_CHECK_BUNDLE" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
